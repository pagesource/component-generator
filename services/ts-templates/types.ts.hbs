import { UseQueryResult, QueryOptions, Query } from 'react-query/types';

export type APIResponse = UseQueryResult;
export interface defaultPropTypes {
  url?: string;
  query?: Query;
  options: RequestInit;
  config?: QueryOptions;
  renderWithResult?: (result: APIResponse) => any;
  isMock: boolean
}
