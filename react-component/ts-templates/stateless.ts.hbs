/**
*
* {{ properCase name }}
*
*/

// Node_Module Imports
import React from 'react';

// Relative imports
import { CompRoot } from './{{ properCase name }}.style';

export interface {{ properCase name }}Props {
    className?: string,
    children?: React.ReactNode
}

const {{ properCase name }}: React.FunctionComponent<{{ properCase name }}Props>  = ({ className, children }: {{ properCase name }}Props) => <CompRoot className={className} data-testid='CompRoot'>
  {children}</CompRoot>;

{{ properCase name }}.defaultProps = {};
export default {{ properCase name }};
export { {{ properCase name }} };
